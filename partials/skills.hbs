{{#if resume.skills.length}}
<div>
  <h3>Skills</h3>
  {{#each resume.skills}}
  <section>
    {{#if name}}
      <h4>{{name}}</h4>
    {{/if}}
    {{#if keywords.length}}
      <ul>
        {{#each keywords}}
        <li>
          {{.}}
        </li>
        {{/each}}
      </ul>
    {{/if}}
  </section>
  {{/each}}
</div>
{{/if}}

